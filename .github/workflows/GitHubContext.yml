name: GitHub Context


on:
 workflow_dispatch
jobs:
 github_context:
  runs-on: ubuntu-latest
  steps:
   - name: Print Info about GitHub
     run: |
      echo 'Repository details ${{github.repository}}'

  
